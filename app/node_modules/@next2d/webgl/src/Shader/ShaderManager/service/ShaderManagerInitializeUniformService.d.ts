import type { IUniformData } from "../../../interface/IUniformData";
/**
 * @description uniform変数の初期化
 *              Initialize uniform variables
 *
 * @param {WebGLProgram} program
 * @param {Map} map
 * @param {boolean} [atlas=false]
 * @method
 * @protected
 */
export declare const execute: (program: WebGLProgram, map: Map<string, IUniformData>, atlas?: boolean) => void;
