import type { IPath } from "../../interface/IPath";
/**
 * @description メッシュのパスの中で指定座標が含まれる線を探す
 *              Find lines in the path of the mesh that contain the specified coordinates
 *
 * @param  {number} x
 * @param  {number} y
 * @param  {IPath} paths
 * @return {number[]}
 * @method
 * @protected
 */
export declare const execute: (x: number, y: number, r: number, paths: IPath) => number[];
