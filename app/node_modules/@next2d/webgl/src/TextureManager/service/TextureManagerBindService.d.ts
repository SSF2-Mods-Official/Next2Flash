import type { ITextureObject } from "../../interface/ITextureObject";
/**
 * @description 指定のunitにテクスチャをバインドします。nullの場合はバインドを解除します。
 *              Binds a texture to the specified unit. If null, the binding is released.
 *
 * @param  {number} index
 * @param  {number} unit
 * @param  {ITextureObject} [texture_object=null]
 * @param  {boolean} [smooth=false]
 * @return {void}
 * @method
 * @protected
 */
export declare const execute: (index: number, unit: number, texture_object?: ITextureObject | null, smooth?: boolean) => void;
