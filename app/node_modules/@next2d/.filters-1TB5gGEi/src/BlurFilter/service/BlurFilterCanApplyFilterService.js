/**
 * @description フィルターを適用できるかどうかを返します。
 *              Returns whether the filter can be applied.
 *
 * @param  {BlurFilter} filter
 * @return {boolean}
 * @method
 * @private
 */
export const execute = (filter) => {
    return filter.blurX > 0 && filter.blurY > 0 && filter.quality > 0;
};
