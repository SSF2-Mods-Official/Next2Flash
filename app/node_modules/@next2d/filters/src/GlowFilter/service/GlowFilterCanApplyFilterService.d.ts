import type { GlowFilter } from "../../GlowFilter";
/**
 * @description フィルターを適用できるかどうかを返します。
 *              Returns whether the filter can be applied.
 *
 * @param  {GlowFilter} glow_filter
 * @return {boolean}
 * @method
 * @private
 */
export declare const execute: (glow_filter: GlowFilter) => boolean;
