import type { Matrix } from "../../Matrix";
/**
 * @description マトリックスを逆変換
 *              Invert the matrix
 *
 * @param  {Matrix} matrix
 * @return {void}
 * @method
 * @public
 */
export declare const execute: (matrix: Matrix) => void;
