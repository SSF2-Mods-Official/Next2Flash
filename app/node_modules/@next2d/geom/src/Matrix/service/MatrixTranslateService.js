/**
 * @description マトリックスの座標移動
 *              Coordinate movement of matrix
 *
 * @param  {Matrix} matrix
 * @param  {number} dx
 * @param  {number} dy
 * @return {void}
 * @method
 * @public
 */
export const execute = (matrix, dx, dy) => {
    matrix.tx += dx;
    matrix.ty += dy;
};
