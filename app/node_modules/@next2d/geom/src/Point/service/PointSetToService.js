/**
 * @description 指定されたポイントの座標を設定します。
 *              Sets the coordinates of the specified point.
 *
 * @param  {Point} point
 * @param  {number} x
 * @param  {number} y
 * @return {void}
 * @method
 * @public
 */
export const execute = (point, x, y) => {
    point.x = x;
    point.y = y;
};
