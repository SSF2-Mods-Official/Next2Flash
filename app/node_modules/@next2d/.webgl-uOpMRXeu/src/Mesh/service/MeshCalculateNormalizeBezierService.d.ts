import type { IPoint } from "../../interface/IPoint";
/**
 * @description ベクトルの正規化を行う
 *              Normalize the vector
 *
 * @param  {IPoint} point
 * @return {IPoint}
 * @method
 * @protected
 */
export declare const execute: (point: IPoint) => IPoint;
