import { ShaderManager } from "../../ShaderManager";
/**
 * @description グラデーションLUTのuniform変数を設定します。
 *              Set the uniform variables of the gradient LUT.
 *
 * @param  {ShaderManager} shader_manager
 * @param  {array} stops
 * @param  {number} begin
 * @param  {number} end
 * @param  {Float32Array} table
 * @return {void}
 * @method
 * @protected
 */
export declare const execute: (shader_manager: ShaderManager, stops: number[], begin: number, end: number, table: Float32Array) => void;
