import type { ShaderManager } from "../../ShaderManager";
/**
 * @description グラデーションフィルターのLUTのuniform変数を設定します。
 *              Set the uniform variables of the gradient filter LUT.
 *
 * @param  {ShaderManager} shader_manager
 * @param  {Float32Array} ratios
 * @param  {Float32Array} colors
 * @param  {Float32Array}alphas
 * @param  {number} begin
 * @param  {number} end
 * @return {void}
 * @method
 * @protected
 */
export declare const execute: (shader_manager: ShaderManager, ratios: Float32Array, colors: Float32Array, alphas: Float32Array, begin: number, end: number) => void;
