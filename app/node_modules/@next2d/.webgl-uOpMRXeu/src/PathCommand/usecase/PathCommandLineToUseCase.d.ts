/**
 * @description 現在の座標から指定の(x,y)座標に直線を描画します。
 *              Draws a straight line from the current coordinates to the specified (x, y) coordinates.
 *
 * @param  {number} x
 * @param  {number} y
 * @return {void}
 * @method
 * @protected
 */
export declare const execute: (x: number, y: number) => void;
