/**
 * @description Quart easing out function
 *
 * @param  {number} t
 * @param  {number} b
 * @param  {number} c
 * @param  {number} d
 * @return {number}
 * @method
 * @public
 */
export const execute = (t, b, c, d) => {
    t /= d;
    return (--t * t * t * t - 1) * -c + b;
};
