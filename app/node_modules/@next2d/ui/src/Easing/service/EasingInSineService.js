/**
 * @description Easing in sine function
 *
 * @param  {number} t
 * @param  {number} b
 * @param  {number} c
 * @param  {number} d
 * @return {number}
 * @method
 * @public
 */
export const execute = (t, b, c, d) => {
    return -c * Math.cos(t / d * (Math.PI / 2)) + c + b;
};
