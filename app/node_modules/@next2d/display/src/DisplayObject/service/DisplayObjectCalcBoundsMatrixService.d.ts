/**
 * @description DisplayObjectのmatrixを考慮した描画範囲を計算
 *              Calculate the drawing range considering the matrix of DisplayObject
 *
 * @param  {number} x_min
 * @param  {number} y_min
 * @param  {number} x_max
 * @param  {number} y_max
 * @param  {Float32Array} matrix
 * @return {array}
 * @method
 * @protected
 */
export declare const execute: (x_min: number, y_min: number, x_max: number, y_max: number, matrix: Float32Array) => Float32Array;
