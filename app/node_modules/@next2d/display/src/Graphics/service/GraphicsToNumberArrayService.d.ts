/**
 * @description Graphicsのrecodesを解析して数値配列を生成します。
 *              Parses Graphics recodes and generates a numerical array.
 *
 * @param  {array} recodes
 * @return {any[]}
 * @method
 * @public
 */
export declare const execute: (recodes: any[] | null) => any[];
