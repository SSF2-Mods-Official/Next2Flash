import type { Shape } from "../../Shape";
/**
 * @description Shapeのローカルバウンディングボックスを取得します。
 *              Get the local bounding box of the Shape.
 *
 * @param  {Shape} shape
 * @return {Float32Array}
 * @protected
 */
export declare const execute: (shape: Shape) => Float32Array;
