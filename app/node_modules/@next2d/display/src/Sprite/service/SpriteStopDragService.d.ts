import type { Sprite } from "../../Sprite";
/**
 * @description startDrag() メソッドを終了して、ドラッグ変数をリセットします。
 *              Ends the startDrag() method and resets the drag variables.
 *
 * @param  {Sprite} sprite
 * @return {void}
 * @method
 * @protected
 */
export declare const execute: <D extends Sprite>(sprite: D) => void;
