/**
 * @description 画像をバッファに変換します
 *              Convert image to buffer
 *
 * @param  {HTMLCanvasElement} canvas
 * @return {Uint8Array}
 * @method
 * @protected
 */
export declare const execute: (canvas: HTMLCanvasElement) => Uint8Array;
