/**
 * @description 画像をバッファに変換します
 *              Convert image to buffer
 *
 * @param  {HTMLImageElement} image
 * @return {Uint8Array}
 * @method
 * @protected
 */
export declare const execute: (image: HTMLImageElement) => Uint8Array;
